# Simple Point of Sales (POS) System
A simple project Prototype POS system for our small sari-sari store, so that it can
easy for us to do an inventory monthly or yearly. Then since it only prototypes, it don't
have a backend just only using it local storage.

# Features (Release Soon....)

